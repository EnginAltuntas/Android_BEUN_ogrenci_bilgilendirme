<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/benm_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/benm_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/buttonshape" />

        <activity android:name=".MainActivity" android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".yemeklistesi" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".duyurular" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".haberler" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".sonradan_acilan" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".menu" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".yemeklistesigiris" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".GirisActivity" android:theme="@style/Theme.AppCompat.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>